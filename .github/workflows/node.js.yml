name: Node.js CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x] # Test across multiple LTS versions

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm' # Caches dependencies for faster builds

    - name: Install dependencies
      run: npm ci # Performs a clean install based on package-lock.json

    - name: Run tests
      run: npm test

    - name: Build project
      run: npm run build --if-present
